rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // PANSAMANTALANG TEST RULE: Allow all reads and writes
    // (Siguraduhin na papalitan mo ito pagkatapos ng pag-test!)
    match /{document=**} {
      allow read, write: if true;
    }

  }
}